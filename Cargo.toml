[workspace]
resolver = "2"
members = [
    "crates/jarvis-core",
    "crates/jarvis-audio",
    "crates/jarvis-wake",
    "crates/jarvis-stt",
    "crates/jarvis-llm",
    "crates/jarvis-tts",
    "crates/jarvis-skills",
    "crates/jarvis-skills-macros",
    "crates/jarvis-mcp",
    "crates/jarvis-desktop",
    "crates/jarvis-service",
    "crates/jarvis-memory",
    "crates/jarvis",
]

[workspace.package]
version = "0.1.0"
edition = "2021"
rust-version = "1.90"
license = "MIT OR Apache-2.0"
repository = "https://github.com/sycatle/local-jarvis"

[workspace.dependencies]
# Async runtime
tokio = { version = "1", features = ["full"] }
tokio-util = { version = "0.7", features = ["rt"] }
async-trait = "0.1"
futures = "0.3"

# Logs / errors
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
tracing-journald = "0.3"
anyhow = "1"
thiserror = "1"

# Serde / config
serde = { version = "1", features = ["derive"] }
serde_json = "1"
schemars = "0.8"
toml = "1.1"
figment = { version = "0.10", features = ["toml", "env"] }

# Dirs / CLI
directories = "5"
clap = { version = "4", features = ["derive"] }

# Audio / DSP
cpal = "0.15"
realfft = "3"
hound = "3"

# ML inference
ort = { version = "=2.0.0-rc.10", default-features = false, features = ["std", "download-binaries", "ndarray"] }
ndarray = "0.16"
whisper-rs = "0.13"
llama-cpp-2 = "0.1"

# TTS phonemization
espeakng-sys = "0.1"

# D-Bus
zbus = { version = "4", default-features = false, features = ["tokio"] }

# Storage / time
rusqlite = { version = "0.31", features = ["bundled"] }
chrono = { version = "0.4", default-features = false, features = ["serde", "clock"] }

# proc-macros
syn = { version = "2", features = ["full", "extra-traits"] }
quote = "1"
proc-macro2 = "1"

# Internal crates
jarvis-core = { path = "crates/jarvis-core" }
jarvis-audio = { path = "crates/jarvis-audio" }
jarvis-wake = { path = "crates/jarvis-wake" }
jarvis-stt = { path = "crates/jarvis-stt" }
jarvis-llm = { path = "crates/jarvis-llm" }
jarvis-tts = { path = "crates/jarvis-tts" }
jarvis-skills = { path = "crates/jarvis-skills" }
jarvis-skills-macros = { path = "crates/jarvis-skills-macros" }
jarvis-mcp = { path = "crates/jarvis-mcp" }
jarvis-desktop = { path = "crates/jarvis-desktop" }
jarvis-service = { path = "crates/jarvis-service" }
jarvis-memory = { path = "crates/jarvis-memory" }

[profile.release]
lto = "thin"
codegen-units = 1
strip = "debuginfo"
